#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {

    double result = pow(2, 3);   // 2³ = 8
    printf("%.0f\n", result); // %.olf

    printf("%.0f\n",  sqrt(4));
    printf("%.2f\n",  ceil(4.65444));
     printf("%.4f\n",  floor(4.65444));
    return 0;
}