#ifndef WEATHER_H // If WEATHER_H is NOT defined yet -> then continue & else skip this file
                    //  mark as included
#define WEATHER_H

/*
A header file is:
 - A place to declare what functions/types exist — not their code.
 - These functions are available to use
*/

void fetchWeather(const char *city);

#endif

