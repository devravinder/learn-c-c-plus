# Weather CLI in C

## Requirements

- gcc
- libcurl

## Install dependency

```bash
sudo apt update
sudo apt install libcurl4-openssl-dev
```

### Build

`make`

### Run

`make run`

## Note:-

- project depency libs are installed globally (on systme level )
  - as C is system level language, this is one way to install dependencies
