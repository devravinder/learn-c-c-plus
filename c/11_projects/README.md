# Projects

## Managing Project

```bash
Dependency manager → Build system → Compiler
```

| Layer           | Tools                    |
| --------------- | ------------------------ |
| 📦 Dependencies | pkg-config, vcpkg, Conan |
| 🏗 Build system  | CMake                    |
| ⚙ Compile       | gcc / clang / MSVC       |

## Project Management Types

- install dependencies at syetm level + CMake
- install dependencies at project level (vcpkg) + CMake    [recommonded]
